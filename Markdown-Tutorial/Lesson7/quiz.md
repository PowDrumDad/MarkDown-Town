# Lesson 7: Quiz

Let's test your understanding of this new concept.

**Question 1:** What is the primary benefit of generating Markdown with a script?
a) It makes the Markdown file look prettier.
b) It allows you to use more complex Markdown syntax.
c) It automates the creation of reports from data, saving time and reducing errors.
d) It's the only way to create tables.

**Question 2:** What is a CSV file?
a) A special type of image file.
b) A file format for storing Markdown text.
c) A Comma-Separated Values file, a simple spreadsheet-like format for data.
d) A compiled Python script.

**Question 3:** In our Python script, what is the general process for creating the report?
a) Read data, open a Markdown file, loop through data writing formatted lines, save the file.
b) Open a Markdown file, manually type the report, save the file.
c) Read data, convert it to an image, and paste it into a document.
d) Create a Python script that is also a Markdown file.

The answers are waiting for you in `solution.md`.
